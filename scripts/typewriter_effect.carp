str Passthrough(str text) -> text + 'X'

void Do(str text) -> for chr c : text -> pw(Passthrough(c))

int Add(int a, int b)
{
    return a + b
}

p('zen' * 'xxx'.length)
int c = Add(1, 2)
p(c)

let col = ['hello', 'world', 'test'] :: .upper
p(col)

# Should output something like this: ['HELLO', 'WORLD', 'TEST']