import std.io
import std.math

# define add with a, b as a + b
#define add(a, b) = (
    a + b
)

IO.println(add(1, 2))
IO.println(add(1,2))

#define true = Math.random(0, 5) > 3

IO.println(true)
IO.println(true)
IO.println(true)
IO.println(true)
IO.println(true)